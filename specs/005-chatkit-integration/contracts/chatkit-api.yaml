# ChatKit API Contract for RAG Integration

## Version: 1.0.0

This document specifies the contract for the ChatKit integration with the RAG backend API.

## Base Path
`/api/v1`

## Integration Pattern
The ChatKit component will communicate with the existing RAG backend using the same API contracts as specified in the original RAG system, ensuring compatibility with the existing backend infrastructure.

## Endpoints

### POST /query
Process a chat message through the RAG system.

#### Request
**Content-Type**: `application/json`

**Body**:
```json
{
  "query": "string (required) - The user's message or question",
  "session_id": "string (optional, UUID format) - Session identifier for conversation context",
  "selected_text": "string (optional) - Text selected by the user that provides additional context",
  "temperature": "number (optional) - LLM temperature parameter (0.0 to 1.0, default: 0.7)",
  "max_tokens": "integer (optional) - Maximum tokens for response (1 to 4096, default: 1024)"
}
```

**Example Request**:
```json
{
  "query": "Can you explain how authentication works in this system?",
  "session_id": "123e4567-e89b-12d3-a456-426614174000",
  "selected_text": "The system uses JWT tokens for authentication. When a user logs in...",
  "temperature": 0.7,
  "max_tokens": 1024
}
```

#### Response
**Success Response (200 OK)**:
```json
{
  "response": "string - The agent's response to the query",
  "sources": [
    {
      "title": "string - Title of the source document",
      "url": "string (optional) - URL to the source document",
      "text_snippet": "string - Relevant text snippet from the source",
      "score": "number - Relevance score (0.0-1.0)"
    }
  ],
  "session_id": "string - The session identifier used",
  "tokens_used": {
    "input_tokens": "integer - Number of tokens in the input",
    "output_tokens": "integer - Number of tokens in the output",
    "total_tokens": "integer - Total number of tokens (input + output)"
  },
  "timestamp": "string - ISO 8601 timestamp of the response"
}
```

**Example Response**:
```json
{
  "response": "The system uses JWT tokens for authentication. When a user logs in, a JWT token is generated and included in subsequent requests...",
  "sources": [
    {
      "title": "Authentication Guide",
      "url": "https://docs.example.com/auth",
      "text_snippet": "The system uses JWT tokens for authentication. When a user logs in...",
      "score": 0.92
    }
  ],
  "session_id": "123e4567-e89b-12d3-a456-426614174000",
  "tokens_used": {
    "input_tokens": 156,
    "output_tokens": 89,
    "total_tokens": 245
  },
  "timestamp": "2025-12-25T10:30:00.000Z"
}
```

**Error Responses**:
- `400 Bad Request`: Invalid request parameters
- `401 Unauthorized`: Invalid or missing API key
- `429 Too Many Requests`: Rate limit exceeded
- `500 Internal Server Error`: Server error during processing
- `503 Service Unavailable`: External service (LLM/Qdrant) unavailable

### GET /query/health
Check the health of the query service.

#### Response
**Success Response (200 OK)**:
```json
{
  "status": "string - Health status (healthy/unhealthy)",
  "service": "string - Service name (query)",
  "agent": "string - Agent name (RAG Chatbot Agent)",
  "timestamp": "string - ISO 8601 timestamp"
}
```

### GET /query/stats
Get query processing statistics.

#### Response
**Success Response (200 OK)**:
```json
{
  "total_queries_processed": "integer - Total number of queries processed",
  "average_response_time_ms": "number - Average response time in milliseconds",
  "success_rate": "number - Success rate as decimal (0.0-1.0)",
  "active_sessions": "integer - Number of active conversation sessions",
  "agent_model": "string - Model being used by the agent",
  "timestamp": "string - ISO 8601 timestamp"
}
```

## Security
- API key authentication required via `Authorization` header
- Requests should be validated for proper content types
- Rate limiting should be enforced per IP/session

## Error Handling
- Errors should follow the common error format:
```json
{
  "error": "string - Error message",
  "error_code": "string - Error code",
  "details": {
    "message": "string - Detailed error message"
  }
}
```

## Session Management
- If no session_id is provided, a new session is created automatically
- Sessions timeout after 30 minutes of inactivity
- Session context is maintained across requests with the same session_id

## ChatKit Specific Considerations
- The API should be compatible with OpenAI's chat completion format if ChatKit requires it
- Messages should be formatted to work with ChatKit's expected input/output structure
- Error responses should be structured to provide user-friendly feedback in the ChatKit interface